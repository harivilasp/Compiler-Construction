int gcd(int a, int b) {
	if ((b == 0)) {
		return a;
	}
	else {
		return call gcd(b, (a % b));
	}
}

int main() {
	
	int a, b;
	call input(a, b);
	int ans = call gcd(a, b);
	call print(ans);

	return 0;
}